// Provides the common blueprint types and JSON schema with reading and writing functions from and to JSON and YAML.
// Parsing is always done in JSON format, for reading and writing YAML the JSON format is used as an intermediate step.
package blueprint
