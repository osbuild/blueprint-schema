---
description: |
  The network protocol used for the connection. This can be either TCP, UDP, ICMP or ANY protocol.

  Empty value is equivalent to `any` for port definitions or is ignored for service definitions.
type: string
default: any
enum:
  - tcp
  - udp
  - any
  - icmp
x-enum-varnames:
  - ProtocolTCP
  - ProtocolUDP
  - ProtocolAny
  - ProtocolICMP
x-go-type-skip-optional-pointer: true
x-omitempty: true
x-go-type-name: NetworkProtocol
x-order: 1
additionalProperties: false
