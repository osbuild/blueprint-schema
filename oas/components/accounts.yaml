---
type: object
description: Operating system user and group accounts to be created on the image.
nullable: true
x-omitempty: true
properties:
  groups:
    type: array
    items:
      "$ref": "accounts_groups.yaml"
    nullable: true
    x-go-type-skip-optional-pointer: true
  users:
    type: array
    items:
      "$ref": "accounts_users.yaml"
    nullable: true
    x-go-type-skip-optional-pointer: true
additionalProperties: false
