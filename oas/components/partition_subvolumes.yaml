---
type: object
properties:
  name:
    description: Subvolume name, must also define its parent volume.
    type: string
  mountpoint:
    "$ref": "storage_mountpoint.yaml"
additionalProperties: false
