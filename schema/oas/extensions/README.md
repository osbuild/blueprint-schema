These snippets or fragments will be merged into components named after extension
filename (without yaml extension). Only the following root properties will be
merged any anything else is ignored:

- oneOf
- anyOf
- allOf
