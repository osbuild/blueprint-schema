---
oneOf:
  - anyOf:
    - required:
        - selected
    - required:
        - unselected
    - required:
        - selected
        - unselected
  - required:
      - json_profile_id
      - json_filepath
