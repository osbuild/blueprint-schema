name: "Blueprint example: fsnodes"
fsnodes:
  - path: "/var/tmp/dir"
    type: "dir"
    contents:
      text: "test" # invalid - directory must not have contents
  - path: "/var/tmp/dir"
    type: "dir"
    contents:
      base64: "test" # invalid - directory must not have contents
